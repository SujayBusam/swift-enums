swift-enums-implementing.pdf is the report.

SwiftEnums is the Xcode project for the simple iOS app mentioned in the report.
The two main files to look at are LoginProvider.swift and ViewController.swift

CodeSamplesPlayground.playground is the directory for the Xcode "playground" file
that contains sample code shown in the report.